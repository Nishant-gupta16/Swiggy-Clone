# Swiggy-Clone
Swiggy Clone (Frontend Only) is a responsive food delivery website UI built using HTML, CSS, and JavaScript. It looks similar to Swiggy’s interface and includes pages like Home, Restaurant Listing, Menu . It focuses only on the design and user interface — no backend, no database, only static UI screens that mimic the real Swiggy experience.
